const initialState = {
    darkMode: false,
}

const siteReducer = (state = initialState, action) => {
    switch (action.key) {
        default:
            return state
    }
}

export default siteReducer
